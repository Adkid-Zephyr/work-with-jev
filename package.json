{
  "name": "work-with-jev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check dist/app.js"
  },
  "dependencies": {
    "@larksuite/cli": "1.0.96"
  },
  "engines": {
    "node": ">=22.9"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  },
  "description": "把工作消息分成四类，用 Jev 找到需要你处理的事。",
  "license": "MIT"
}
