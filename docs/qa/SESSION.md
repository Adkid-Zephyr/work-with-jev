# 续接入口 / Maintenance handoff

状态：本轮两次 Computer Use 评测与修复已完成。详细记录：[REVIEW.md](REVIEW.md)。

## 不变的约束

- 保持现有极简界面和核心功能；不要擅自增加导航、统计、自动回复、新平台接入或后台监控。
- 测试使用隔离端口和虚构消息；实际账号/模型验证与离线测试分开记录。
- 不提交 `.data`、密钥、授权二维码、真实聊天和个人浏览器存储。
- 原始消息、队列状态、手动分类都是用户资产，不用重置状态来掩盖 bug。

## 已完成

- 修复队列详情改分类不回写原消息、弹窗错误被遮挡、搜索按钮挤压、显示时区不一致。
- 异步操作锁定可变控件；恢复时保留原 disabled 状态。
- 修复键盘勾选失焦，保留队列滚动位置。
- 卡片正文与操作分行，不再挤压阅读宽度。
- Chrome 实测桌面与 390px 窄屏；实际拖动、连续键盘勾选、刷新持久化通过。
- 19 项自动测试通过。

## 下次如何续接

先读取本文件和 REVIEW，再核实 `git status`、当前服务和用户的具体复现步骤。不把此前通过的自动测试当作实际飞书/模型端到端通过。重新复现新问题后再修改，并只补相关回归验证。

尚未在本轮验证：真实群搜索、原生客户端消息定位、Jev 分类质量、规模化跨批上下文。没有运行中的自动迭代任务，也没有剩余的承诺性后台工作。

## 企业微信接入更新

新增官方智能机器人长连接适配器；前端来源与身份隔离。见 docs/WECOM.md。模拟接收测试通过，真实端到端验收仍需用户 Bot ID / Secret。不支持未 @机器人的群消息或完整历史拉取。
